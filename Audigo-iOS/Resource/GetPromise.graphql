query GetPromise($id: ID!) {
  promise(id: $id) {
    id
    address
    timestamp
    latitude
    longitude
    name
    pockets {
      phone
      latitude
      longitude
      pushToken
      profileImagePath
      nickname
    }
  }
}
